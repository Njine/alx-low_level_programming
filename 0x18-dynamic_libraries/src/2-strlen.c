#include <stdio.h>
#include "main.h"
/**
 * _strlen - Calculate the length of a string.
 * @s: Pointer to the string.
 *
 * This function calculates the length of the input string by
 * iterating through the characters until the null terminator
 * is encountered.
 *
 * Return: The length of the string as an integer.
 */

int _strlen(char *s)
{}
