#include "main.h"
#include <stdio.h>
/**
* _memset - Fill a memory area with a specified byte value.
* @s: Pointer to the memory area to be filled.
* @b: The byte value used for filling the memory area.
* @n: The number of bytes to fill.
* Return: A pointer to the memory area after filling.
*/
char *_memset(char *s, char b, unsigned int n)
{}
